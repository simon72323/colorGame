'use strict';

var urlHelper = require('../urlHelper.js');

class UserBehavior {
  static get shared() {
    var _a, _b;
    if ((_a = window.gtLibTsScope) == null ? void 0 : _a.userBehavior) {
      return window.gtLibTsScope.userBehavior;
    }
    return ((_b = window.gtLibTsScope) != null ? _b : window.gtLibTsScope = {}).userBehavior = new UserBehavior();
  }
  sendGtagEvent(eventName, data = {}) {
    var _a;
    (_a = window.gtag) == null ? void 0 : _a.call(window, "event", `customEvt_${eventName}`, {
      ...data,
      GameType: urlHelper.UrlHelper.shared.gameType
    });
  }
}

exports.UserBehavior = UserBehavior;
//# sourceMappingURL=userBehavior.js.map
