function getUrlQuery(name) {
  var _a;
  const formatName = name.replace(/([[\]])/g, "\\$1");
  const reg = new RegExp(`[\\?&]${formatName}=([^&#]*)`);
  const result = reg.exec(location.search);
  return decodeURIComponent(((_a = result == null ? void 0 : result[1]) != null ? _a : "").replace(/\+/g, " "));
}
function getCookie(cookieName) {
  var _a, _b;
  const decodedCookie = decodeURIComponent(document.cookie);
  const reg = new RegExp(`(^|;\\s?)${cookieName}=([^;]*)(;|$)`);
  return (_b = (_a = reg.exec(decodedCookie)) == null ? void 0 : _a[2]) != null ? _b : "";
}

export { getCookie, getUrlQuery };
//# sourceMappingURL=urlUtils.mjs.map
