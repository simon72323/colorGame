export interface ICrypto {
    encrypt(data: any): string;
    decrypt(data: any): string;
}
export declare class AESCrypto implements ICrypto {
    private secret;
    private key;
    private iv;
    constructor(secret: string);
    init(secret?: string): void;
    encrypt(_data: any): string;
    decrypt(data: string): string;
}
