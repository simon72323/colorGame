import { computed } from '../../utils/reactivity/index.mjs';
import { reactive } from '../../utils/reactivity/vue-reactivity.mjs';

function useGetters(state) {
  return reactive({
    bet: computed(() => state.line * state.lineBet)
  });
}

export { useGetters };
//# sourceMappingURL=getters.mjs.map
